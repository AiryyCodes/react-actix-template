# === Database Configuration ===
DB_USER=website_template               # Database username
DB_PASSWORD=website_template           # Database password
DB_DATABASE=website_template           # Database name
DB_HOST=127.0.0.1                      # Database host (localhost)
DB_PORT=5432                           # PostgreSQL default port

# Full database connection string used by backend (Actix + SQLx)
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}

# === App URLs ===
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080     # URL where Actix backend is running
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000    # URL where Next.js frontend is running

# === JWT Secret ===
JWT_SECRET=my_secret                   # Secret used for signing JWT tokens (change this in production!)

